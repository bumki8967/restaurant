<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper 
   PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN"
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
   
   
<mapper namespace="com.project.restaurant.user.UserInfoService">
	<resultMap type="com.project.restaurant.user.UserInfo" id="UserInfo">
		<result property="user_seq"			column="user_seq" />
		<result property="user_id"			column="user_id" />
		<result property="user_pw"			column="user_pw" />
		<result property="name"				column="name" />
		<result property="email"			column="email" />
		<result property="tel"				column="tel" />
		<result property="birthday"			column="birthday" />
		<result property="gender"			column="gender" />
		<result property="postcode"			column="postcode" />
		<result property="addr1"			column="addr1" />
		<result property="addr2"			column="addr2" />
		<result property="reg_date"			column="reg_date" />
		<result property="type"				column="type" />
		<result property="etc1"				column="etc1" />
		<result property="etc2"				column="etc2" />
		<result property="etc3"				column="etc3" />
	</resultMap>



	<!-- 회원가입 (INSERT) -->
	<insert id="insertUser" parameterType="com.project.restaurant.user.UserInfo"
		useGeneratedKeys="true" keyProperty="user_seq">
		INSERT INTO user_info (
			user_id, user_pw, name, email, tel, birthday, gender, 
			postcode, addr1, addr2, reg_date, type, etc1, etc2, etc3
		) VALUES (
			#{user_id}, #{user_pw}, #{name}, #{email}, #{tel}, #{birthday}, #{gender}, 
			#{postcode}, #{addr1}, #{addr2}, #{reg_date}, #{type}, #{etc1}, #{etc2}, #{etc3}
		)
	</insert>	
		
		
	<!-- 중복회원가입 방지를 위해 아이디 중복검사 수행 -->
	<select id="dupliecationUserId" parameterType="com.project.restaurant.user.UserInfo" resultType="int">
		SELECT	count(user_id)
		FROM	user_info
		WHERE	user_id = #{user_id}
	</select>	
	
	
	<!-- 중복회원가입 방지를 위해 이메일 중복검사 수행 -->
	<select id="duplicationEmail" parameterType="com.project.restaurant.user.UserInfo" resultType="int">
		SELECT	count(email)
		FROM	user_info
		WHERE	email = #{email}		
	</select>
</mapper>